/** HttpOnly cookie storing the app user id (`public.users.id`). */
export const USER_ID_COOKIE = "sakan_user_id";

/** HttpOnly cookie storing the app room code (Meez code). */
export const ROOM_CODE_COOKIE = "sakan_room_code";

export const USER_ID_COOKIE_MAX_AGE = 60 * 60 * 24 * 365; // 1 year
